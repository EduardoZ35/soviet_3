<Window x:Class="RelojControl.Windows.WndPanelControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" ResizeMode="NoResize"
        WindowState="Maximized" Background="{StaticResource BgBrush}">
  <Grid>
    <Viewbox Stretch="Uniform">
      <Grid Width="1366" Height="768">
        <DockPanel>
      <!-- Header -->
      <Border DockPanel.Dock="Top" Background="{StaticResource AccentBrush}" Padding="28,16">
        <Grid>
          <TextBlock Text="Panel de Control · rFlex"
                     FontFamily="{StaticResource FontPoppins}" FontWeight="Bold" FontSize="18"
                     Foreground="White" VerticalAlignment="Center"/>
          <Button Content="Cerrar" HorizontalAlignment="Right"
                  Background="Transparent" Foreground="White" BorderBrush="Transparent"
                  FontFamily="{StaticResource FontPoppins}" FontSize="13" Padding="16,8"
                  Cursor="Hand" Click="BtnCerrar_Click"/>
        </Grid>
      </Border>

      <!-- Sidebar -->
      <Border DockPanel.Dock="Left" Width="200" Background="{StaticResource Surface2Brush}"
              BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,1,0">
        <StackPanel Margin="0,12,0,0">
          <Button Content="Enrolamiento" Click="BtnEnrolamiento_Click"
                  Padding="20,14" HorizontalAlignment="Stretch"
                  HorizontalContentAlignment="Left" BorderThickness="0"
                  Background="Transparent"
                  FontFamily="{StaticResource FontPoppins}" FontSize="13"
                  Foreground="{StaticResource InkBrush}" Cursor="Hand"/>
        </StackPanel>
      </Border>

      <!-- Main content -->
      <Grid Background="{StaticResource BgBrush}">
        <TextBlock Text="Selecciona una opción del menú lateral"
                   Style="{StaticResource TextSubtitle}"
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
      </Grid>
        </DockPanel>
      </Grid>
    </Viewbox>
  </Grid>
</Window>
